<div id="<%= dom_id(discussion) %>" class="mb-2">
  <h2><%= link_to discussion.name, discussion_path(discussion) %></h2>
  Pinned <%= discussion.pinned %> | Closed: <%= discussion.closed%>
  <div>
    <%= link_to "Edit", edit_discussion_path(discussion) %>
    <%= link_to "Delete", discussion_path(discussion), data: { turbo_method: :delete, turbo_confirm: "Are you sure?" } %>
  </div>
</div>
